# === Image reference (GHCR) ===
# Set to the image your GitHub Actions workflow publishes.
# Example: ghcr.io/<owner>/<repo>:latest
IMAGE_REF=ghcr.io/your-github-username/your-repo:latest

# === Discord ===
DISCORD_TOKEN=
# GUILD during development (fast updates), GLOBAL for production.
BOT_SYNC_SCOPE=GUILD
# Your test guild ID (string). Leave empty when BOT_SYNC_SCOPE=GLOBAL.
DEV_GUILD_ID=

# Optional logging/alert channels (string IDs)
LOG_CHANNEL_ID=
ALERT_CHANNEL_ID=
ADMIN_ROLE_ID=

# Auto-delete (minutes) for public messages generated by commands (blank to disable)
AUTO_DELETE_MINUTES=

# === Pterodactyl ===
# Base URL of the Pterodactyl panel (e.g., https://panel.example.com)
PTERO_PANEL_URL=
# Application API key (for admin/estate visibility commands)
PTERO_APP_API_KEY=
# Optional fallback Client API key (use per-user keys via /link instead)
PTERO_CLIENT_API_KEY=

# === Storage & Security ===
# SQLite path (or any SQLAlchemy URL). The container uses /data by default.
DATABASE_URL=sqlite+aiosqlite:///data/bot.db
# 32-byte (64-hex) key to encrypt user tokens (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=
# Purge user-linked keys that haven't been used in N days (default 7)
KEY_PURGE_DAYS=7
